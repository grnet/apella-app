{{gen-form
  formModel
  flat=true
  fieldsetFieldsComponent='paper-content'
  onSubmit=(action 'onSubmit')
  messages=(hash success=(t 'password.forgot.success'))
  labels=(hash submit=(t 'password.forgot.submit'))
  modelMeta=(gen-resource-meta modelMeta)
}}

